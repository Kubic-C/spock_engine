#include "physics.hpp"
#include "state.hpp"

namespace spk {
    void physics_system_tick(flecs::entity e) {
    }

    void physics_cs_init(system_ctx_allocater_t& ctx_alloc, flecs::world& world) {
        spk_trace();        
    }
}